import { SuperstringPipe } from './superstring.pipe';

describe('SuperstringPipe', () => {
  it('create an instance', () => {
    const pipe = new SuperstringPipe();
    expect(pipe).toBeTruthy();
  });
});
